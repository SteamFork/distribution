#!/bin/bash
. /etc/os-release

if [[ $EUID -ne 0 ]];
then
    echo "This must be ran via sudo."
    exit 1
fi

echo -n "Enter the version and branch you would like to opt-in to (${STEAMOS_VERSION}/${VARIANT_ID}): "
read version
echo $version > /etc/manual-enroll-override