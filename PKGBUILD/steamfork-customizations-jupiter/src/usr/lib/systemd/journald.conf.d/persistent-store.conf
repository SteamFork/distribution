# The default option is auto.
#
# Which means that "persistent" will be used if /var/log/journal/ is
# available otherwise it falls back to "volatile", storing the logs
# in /run/log/journal.
#
# Explicitly select persistent for our needs.

[Journal]
Storage=persistent
